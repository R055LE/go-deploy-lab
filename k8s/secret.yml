# Placeholder — in production, use SealedSecrets or External Secrets Operator.
# This file exists to document the required secret shape.
# DO NOT commit real credentials.
apiVersion: v1
kind: Secret
metadata:
  name: go-deploy-lab-db
  namespace: go-deploy-lab
type: Opaque
stringData:
  DATABASE_URL: "postgres://app:changeme@postgres:5432/configstore?sslmode=disable"

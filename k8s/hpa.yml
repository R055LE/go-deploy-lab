apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: go-deploy-lab
  namespace: go-deploy-lab
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: go-deploy-lab
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
